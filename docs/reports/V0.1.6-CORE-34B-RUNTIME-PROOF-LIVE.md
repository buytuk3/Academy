# BuyTuk.V.01.6 — تقرير الإثبات الحي CORE-34B / P2-VOICE Runtime Proof

- **تاريخ التقرير:** 2026-09-14
- **وقت UTC الدقيق الملتقط:** 2026-09-14T08:59:22.764054+00:00
- **Unix timestamp:** 1789376362
- **النسخة قيد الإثبات:** BuyTuk.V.01.6
- **شجرة الإثبات:** `/home/user/core34b_runtime/extracted`
- **الهدف:** إثبات ما إذا كان مسار CORE-34B يعمل تشغيلًا حيًا من واجهة الطالب حتى نقطة الرفع/الإرسال والطابور، مع تسجيل النجاح أو الحظر بنص الخطأ الحقيقي فقط.

## 1) نطاق الإثبات
يشمل هذا التقرير:
1. إعادة فك الحزمة المعتمدة V0.1.6 داخل بيئة التنفيذ والتحقق من SHA-256.
2. التحقق من وجود مسارات CORE-34A الخاصة بـ voice intake وطلب presigned upload داخل الشيفرة المستخرجة.
3. تشغيل الخدمة حيًا وضرب نقاط النهاية الفعلية التالية: تسجيل دخول الطالب، presign، إنشاء attempt، submit.
4. فحص أثر التنفيذ في حالة الـ attempt وحالة صف التحليل.
5. توثيق أي فشل/حظر فعلي كما ظهر في stdout / HTTP / الاستثناءات.

## 2) البيئة
- Node.js: v22.23.2 (ظهر في stack traces المسجلة).
- مدير الحزم: pnpm (نجح التثبيت سابقًا داخل نفس مسار الإثبات).
- Redis: ظهر اتصال ناجح في سجلات التشغيل.
- قاعدة البيانات: PostgreSQL على `127.0.0.1:5432/core32_verify` مستخدمة بواسطة سكربت البذر والتفتيش.
- واجهة API الحية: `http://127.0.0.1:4100`

## 3) التحقق من سلامة الحزمة واستخراجها
**الحالة:** PASS

**الدليل المرصود:**
- تم تنزيل الحزمة المعتمدة: `buytuk-academy-V.01.6-full-2026-09-14.tar.gz`
- SHA-256 المرصود للحزمة: `4e29ad83d88e17469702b42c83528a8f958dd1137457b8b02f61ac24eb310181`
- نتيجة الفحص: **مطابق للقيمة المتوقعة**.
- بعد الفك: `git status` كان نظيفًا و `HEAD = 3b3ad2fb41f2fa859ab0bd27299af2b47ff3a31b` في الشجرة المستخرجة قبل إضافة أدوات الإثبات.

## 4) التحقق من وجود مسارات CORE-34A في الشيفرة
**الحالة:** PASS

**الدليل المرصود من الفحص النصي للشيفرة:**
- `apps/api/src/public/index.html` يحتوي على عنصر الرفع الصوتي: `input id="activity-audio-file"`
- `apps/api/src/public/app.js` يحتوي على طلب presign إلى: `/api/audio/presign?...`
- `apps/api/src/public/app.js` يحتوي على تتبع `voiceUploadKey` بعدة مواضع
- `apps/api/src/public/app.js` يحتوي على POST حقيقي إلى `/v1/attempts/:id/submit`

**بصمات SHA-256 للملفات المثبتة:**
- `apps/api/src/public/app.js`: `3bf5274b23ba96b22852c8471f2f87d2237c8188fcb95e6c90bafd8b8d65e4a1`
- `apps/api/src/public/index.html`: `28773a15739ff7f58afd63f0e1f0a07def2cfae13d6cec28cac40cd7296a8583`
- `apps/api/src/public/styles.css`: `bd01d48ccaf52d2425dcecf6bfe91a75b32c26a8363aff233a1047763fc325d5`
- `package.json`: `717d4e2c3cfcf51a9aa1d67ae9462b26ba7260e7bbc03a64b52d56c3f1fa3851`

## 5) حالات الاختبار الحية

### TC-01 — إقلاع خدمة API الحية
**الحالة:** PASS

**الأمر المنفذ:** تشغيل خدمة API على المنفذ 4100 ثم probe للجذر.

**المرصود:**
- سجل التشغيل أظهر أن الخدمة استمعت على المنفذ `4100`.
- Probe للجذر أعاد `HTTP/1.1 200 OK` مع رؤوس الحماية والـ rate-limit.

### TC-02 — تسجيل دخول الطالب عبر `/v1/auth/student-login`
**الحالة:** PASS

**نص الطلب المرسل:**
```json
{"identityId": "0aa3f753-c683-4f5b-a292-6911a1ea4179"}
```

**السطر المرصود من HTTP:**
```text
HTTP/1.1 200 OK
Date: Mon, 14 Sep 2026 08:57:33 GMT
```

**مقتطف من الاستجابة المرصودة:**
```json
{"context":{"allowed":true,"studentId":"d5784403-ca53-4e70-959a-c61978a4d6eb"},"accessToken":"<redacted in report body>","refreshToken":"<redacted in report body>"}
```

**ملاحظة:** نجاح تسجيل الدخول تحقق فقط بعد تصحيح صياغة JSON في جسم الطلب. المحاولات السابقة فشلت بخطأ parsing، ولذلك لا تُحتسب نجاحًا.

### TC-03 — طلب presigned upload عبر `/api/audio/presign`
**الحالة:** FAIL / BLOCKED

**السطر المرصود من HTTP:**
```text
HTTP/1.1 500 Internal Server Error
Date: Mon, 14 Sep 2026 08:57:41 GMT
```

**نص الخطأ المرصود حرفيًا:**
```json
{"error":"Missing credentials in config, if using AWS_CONFIG_FILE, set AWS_SDK_LOAD_CONFIG=1"}
```

**الاستنتاج:** مسار presign موجود ويُستدعى حيًا، لكن إثبات الرفع الفعلي blocked بسبب غياب بيانات اعتماد AWS/S3 في بيئة التشغيل الحالية.

### TC-04 — إنشاء attempt عبر `/v1/attempts`
**الحالة:** PASS

**جسم الطلب المرصود:**
```json
{"activityId": "f3daf894-819b-4f1a-b6a2-9fede67add28", "exerciseId": "f3daf894-819b-4f1a-b6a2-9fede67add28", "attemptNumber": 1}
```

**السطر المرصود من HTTP:**
```text
HTTP/1.1 201 Created
Date: Mon, 14 Sep 2026 08:57:42 GMT
```

**الاستجابة المرصودة:**
```json
{"attempt":{"id":"7812fa5c-e1f0-4505-a478-3201743ee1ab","state":"STARTED","startedAt":"2026-09-14T08:57:42.012Z"},"created":true}
```

### TC-05 — Submit لمحاولة صوتية عبر `/v1/attempts/:id/submit`
**الحالة:** PASS جزئيًا

**جسم الطلب المرصود:**
```json
{"durationMs":1500,"engineInput":{"passageId":"5aaf174a-6444-4a93-97d4-46be630cf536","sessionId":"25b70d30-97b4-4760-a28c-b9fdb86dc778","audioKey":"student-ui/0557a57c-4d51-4c53-98e6-29de3d1ef16a/d5784403-ca53-4e70-959a-c61978a4d6eb/1789376252-runtime-proof.wav","expectedText":"مرحبا بكم في منصة باي توك التعليمية"}}
```

**السطر المرصود من HTTP:**
```text
HTTP/1.1 200 OK
Date: Mon, 14 Sep 2026 08:57:42 GMT
```

**الاستجابة المرصودة:**
```json
{"id":"7812fa5c-e1f0-4505-a478-3201743ee1ab","state":"SUBMITTED","submittedAt":"2026-09-14T08:57:42.065Z","durationMs":1500}
```

**الاستنتاج:** بوابة الطالب استطاعت إرسال حمولة صوتية منطقية تحتوي `audioKey + passageId + sessionId + expectedText` وتحويل الـ attempt إلى `SUBMITTED` حيًا.

### TC-06 — فحص أثر الإرسال في سجل المحاولة/صف التحليل
**الحالة:** FAIL / BLOCKED

**المرصود من فحص attempt row:**
```json
{
  "found": true,
  "sessionId": "25b70d30-97b4-4760-a28c-b9fdb86dc778",
  "studentId": "d5784403-ca53-4e70-959a-c61978a4d6eb",
  "passageId": "5aaf174a-6444-4a93-97d4-46be630cf536",
  "audioKey": "student-ui/0557a57c-4d51-4c53-98e6-29de3d1ef16a/d5784403-ca53-4e70-959a-c61978a4d6eb/1789376252-runtime-proof.wav",
  "jobId": null,
  "jobStatus": "queued",
  "createdAt": "2026-09-14T08:57:42.068Z"
}
```

**خطأ فحص job status حرفيًا:**
```text
Error: JOB_ID is required
```

**الاستنتاج:** تم تسجيل أثر submit في طبقة الحالة/الصف، لكن الإثبات الحي لم يثبت انتقالًا قابلًا للتفتيش عبر `jobId` لأن الحقل بقي `null` وقت الفحص.

### TC-07 — مسار العامل Worker / تنزيل الصوت من S3
**الحالة:** FAIL / BLOCKED

**الأخطاء المرصودة حرفيًا من السجلات السابقة ضمن تشغيل CORE-34B:**
```text
CredentialsError: missing credentials in config
```
و
```text
Missing credentials in config, if using AWS_CONFIG_FILE, set AWS_SDK_LOAD_CONFIG=1
```

**الاستنتاج:** سبب الحظر الأساسي لإثبات CORE-34B الكامل هو غياب اعتماد S3/AWS اللازم لتوليد رابط presign ثم تنزيل الملف الصوتي في مراحل المعالجة.

## 6) نتيجة الإثبات النهائي
| العنصر | النتيجة | الدليل المختصر |
|---|---|---|
| سلامة حزمة V0.1.6 وفكها | PASS | SHA-256 مطابق، وHEAD مرصود |
| وجود مسارات CORE-34A في الشيفرة | PASS | عناصر upload/presign/submit موجودة نصيًا |
| إقلاع API حيًا | PASS | `HTTP/1.1 200 OK` للجذر |
| تسجيل دخول الطالب | PASS | `HTTP/1.1 200 OK` |
| presign endpoint | FAIL / BLOCKED | `HTTP/1.1 500 Internal Server Error` + missing credentials |
| إنشاء attempt | PASS | `HTTP/1.1 201 Created` |
| submit attempt | PASS جزئيًا | `HTTP/1.1 200 OK` والحالة `SUBMITTED` |
| إثبات worker → storage → STT الكامل | FAIL / BLOCKED | غياب credentials و`jobId: null` وقت الفحص |

## 7) حالة Git داخل شجرة الإثبات وقت التقرير
**الحالة:** DIRTY

**المخرجات المرصودة من `git status --short`:**
```text
?? scripts/
?? tests/core-32/core34b-runtime-proof.test.ts
```

**HEAD المرصود:**
```text
3b3ad2fb41f2fa859ab0bd27299af2b47ff3a31b
```

## 8) الخلاصة التنفيذية
الإثبات الحي أثبت **نجاح الجزء الأمامي والـ API حتى submit**: تسجيل الدخول، إنشاء attempt، وتحويل المحاولة إلى `SUBMITTED` ببيانات صوتية فعلية. لكنه **لم يثبت CORE-34B كاملًا من browser → storage → worker → gateway/STT** لأن نقطة `presign` فشلت حيًا بخطأ اعتماد AWS/S3، كما أن `jobId` بقي `null` وقت التفتيش، وبالتالي لا يوجد إثبات حي مكتمل لمرحلة worker/STT في هذه البيئة.

**الحكم النهائي:** `CORE-34B = BLOCKED` في البيئة الحالية، مع إثبات جزئي ناجح لمسار intake + submit فقط.
