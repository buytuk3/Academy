# CORE26C — RECOVERY PATCH REPORT
Type: **Documented Recovery Patch** (reproducibility only — NOT a functional change).
Baseline (Original HEAD): `d60ac04b4851b985930a342165ca5071382a0d84` (tag `core26c-zib-baseline`).
Recovery-Patch Baseline (fix5): `d96b1bfaa26476419653b6f1b0ab0b38747f6e72` (tag `core26c-zib-recovery-fix5`).
Classification of every defect below: **Repository / Build Reproducibility Defect — NOT a product-logic defect.** The original environment ran solely because manually-accumulated `node_modules` layers (outside version control) papered over these manifest gaps; proven by fresh-extract installs failing on the pristine baseline bytes.

| Fix | Commit | File(s) affected | Original state | Change | Why (evidence) | Rebuild impact | Verification before → after |
|---|---|---|---|---|---|---|---|
| fix1 | `b7420b5` | `pnpm-workspace.yaml` | Stray top-level scalar `apps/*` after `overrides:` (128-line file, non-standard tail) | Removed the stray line; registered `engines/*` under `packages:`; normalized LF | `pnpm install` on fresh extract died parsing the YAML: "can not read a block mapping entry (129:1)" | Workspace file becomes parseable; engine manifests (previously invisible) enter the install graph | install exit 1 → parse OK |
| fix2 | `2ec4919` | `apps/api/package.json`, `apps/worker/package.json` | Deps referenced non-existent workspace names `@workspace/database`, `@workspace/contracts` | Renamed to actual package names `@workspace/db`, `@buytuk/contracts` (manifest-only) | Fresh install: `ERR_PNPM_WORKSPACE_PKG_NOT_FOUND` for apps/api; zero source imports use the wrong names (only hit = a logger name string in packages/database/src/client.ts) | Names resolve; workspace linking proceeds | install exit 254→1→0 across the chain |
| fix3 | `18f185f` | `pnpm-workspace.yaml` (catalog), `packages/events/package.json`, `packages/database/package.json`, `package.json` (root) | Catalog `drizzle-orm ^0.45.2` + `@types/node ^25.3.3`; events `drizzle-orm ^0.36.0`; database declared `drizzle-kit ^0.31.10` while importing undeclared `postgres`; vitest undeclared where used | Aligned to the ACTUALLY TESTED versions (store ground truth: drizzle-orm **0.31.4**, drizzle-kit **0.22.8** — the version CORE-24 tools document, vitest **1.6.1**, @types/node **20.19.43**); declared `postgres` + `vitest`; hoisted drizzle-orm/vitest/@types/node/tsx at root for the four source-only capability packages (decisions/intelligence/learning-loop/curriculum — consumed via tsconfig paths, no manifests) | Fresh-install TSC: two divergent drizzle-orm instances → TS2345/TS2769 overload conflicts in packages/events; TS2307 for `postgres`; test script had no vitest | Single drizzle family; declared-everything installs | events TSC fail → green |
| fix4 | `579f2b5` | `packages/database/node_modules` (tracked symlink → engine store) | A node_modules SYMLINK tracked in version control | Removed from index and tree | Fresh install: pnpm followed the symlink and linked the database package's deps INTO the engine store, leaving `apps/api/node_modules` empty (proven); made the original env irreproducible | Install owns its links; no VCS-tracked build artifacts | apps/api deps unlinked → linked |
| fix5 | `d96b1bf` | `packages/observability/package.json`, `packages/security/package.json` | `observability/src/http.ts` imports express types without declaring express/@types/express; security uses bcryptjs/jsonwebtoken without @types | Declared devDependencies at tested store versions (@types/express 4.17.25, @types/bcryptjs 2.4.6, @types/jsonwebtoken 9.0.10) | Fresh-install TSC TS2307/TS7016 exactly at those imports | Type resolution self-contained | TSC errors → green |
| fix6 | `ccbae42` | `pnpm-workspace.yaml` | `apps/*` NEVER listed under `packages:` — pnpm never treated apps/api & apps/worker as members; fresh install linked none of their deps (empty apps/api/node_modules, 15×TS7016 on express) | Added `- apps/*` as first `packages:` entry | Committed bytes prove it (git show HEAD:pnpm-workspace.yaml); membership now: buytuk-api + buytuk-worker listed by `pnpm -r list` | **apps become first-class install targets** | apps unlinked → express resolves into .pnpm store |
| fix7 | `b84db49` | `apps/api/package.json` | express 5.2.1 dependency with NO `@types/express` (original env carried @types/express 5.0.6 manually in apps/api/node_modules/@types) | devDependencies: @types/express ^5.0.6, @types/cors ^2.8.19, @types/node ^20.12.0 | 15/15 fresh TSC errors in apps/api reduce to the single undeclared 'express' module (TS7016×13 + TS7006 cascade) | Type-only | apps/api TSC fail → green |
| fix8/8b | `3d1a59d` / `66c8f20` | `pnpm-lock.yaml` (first in repo history) | No lockfile — installs were non-deterministic | Committed the clean-generated lockfile; regenerated against the full manifest set (fix7) | Owner gate: "package.json + pnpm-lock.yaml complete" | Deterministic rebuilds; `--frozen-lockfile` becomes enforceable | absent → present & consistent |

PostgreSQL note (environment, not repo): local verification used a TEMPORARY `trust` auth on the isolated test cluster — backed up `pg_hba.conf` first, applied to the verification environment only, restored after tests, and documented in `ZIB_VERIFICATION.md`. No password or trust setting is or was inside any package.

### Runtime Bootstrap Patch (owner-directed independent-boot gate, 2026-09-12)
| Fix | Commit | File(s) affected | Original state | Change | Why (evidence) | Rebuild impact | Verification before → after |
|---|---|---|---|---|---|---|---|
| fix9 | `4455fa0` | NEW `packages/{curriculum,decisions,intelligence,learning-loop}/package.json` + consumer deps in `apps/{api,worker}/package.json` | The four capability packages had NO manifest (nearest = root, which has no `"type"`) → Node/tsx transformed their sources as CommonJS while the importing app is ESM → boot-time link error: `The requested module '@workspace/curriculum' does not provide an export named 'curriculumEvidenceLink'` (raw log preserved: `api-boot-failure-fix8b-raw.log`); tsc/vitest never hit it because they resolve via tsconfig paths/vitest aliases against source without executing (Vitest-specific alias dependency — now eliminated) | One consistent modular pattern: real manifests (`name @workspace/<pkg>`, private, `type: module`, `main`/`types`/`exports` → `./src/index.ts`) + declared as `workspace:*` dependencies of their runtime consumers — standard workspace graph, no symlinks, no logic change | Minimal repro in fresh extract: `import {curriculumEvidenceLink} from "./packages/curriculum/src/index.js"` → `PROBE_REL_OK function`; named import via package specifier → the boot error above | Runtime resolution unified with every other internal package | API/Worker boot fail → **API boots independently** (`Server listening`, `/api/healthz` ⇒ `{"status":"ok"}`), **Worker boots independently** (`Analyze worker ready`, pulled a REAL queued job), runtime probe `RESOLVE_OK: function×4` |
| fix10 | `4951fbb` | `pnpm-lock.yaml` | Lockfile (fix8b) predated the fix9 manifests → out of sync (frozen install would fail) | Regenerated by the clean install of the fix9 tree in the fresh-extracted verification dir | `git diff` of lockfile across fix8b→fix9 non-empty; 8 references to the four new packages in the regenerated lock | Deterministic install incl. the four new manifests | out-of-sync → in-sync (frozen install green on fresh extraction) |

**Boot-verification hygiene note (documented, not a defect):** the independent API/Worker boots left stray `tsx` processes sharing the SAME Redis; a leaked REAL job (S3-credentials error outside vitest mocks) then stayed queued and poisoned the next regression run (core-25 T7/core-26 W1 saw `SUBMITTED` instead of `EVIDENCE_RECORDED`). After killing the strays and `FLUSHDB` (documented verification hygiene), core-25 12/12 and core-26 20/20 passed on the SAME fix9 tree with fresh re-migrated DBs. The BullMQ queue is intentionally shared/distributed — verification runs must isolate or flush it.
